/* module.exports = {
  css: {
    loaderOptions: {
      css: {
        // 这里的选项会传递给 css-loader
        importLoaders: 1,
      },
      sass: {
        // 这里的选项会传递给 postcss-loader
        prependData: `@import "@/style.scss";`
      }
    }
  }
}; */